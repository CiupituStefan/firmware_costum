name: Build ZMK firmware

on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    secrets: inherit
    with:
      matrix: '[{"board":"nice_nano_v2","shield":"ciupitu_stefan"}]' # JSON-ul pe o singură linie
      # west_args: "-p" # Exemplu de alt input, dacă îl ai
