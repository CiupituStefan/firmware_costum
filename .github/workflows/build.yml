name: Minimal ZMK Build Test

on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    secrets: inherit
    # NU adăugăm niciun bloc 'with:' sau lăsăm un 'with: {}' gol.
    # Acest lucru forțează workflow-ul reutilizabil să folosească valorile implicite
    # pentru TOATE inputurile sale, inclusiv pentru 'matrix'.
    # Poți adăuga un with gol dacă preferi:
    # with: {}
